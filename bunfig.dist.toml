[test]
# Coverage is off for the dist-target run (`bun run test:dist`), which loads
# ../dist/*.js instead of the .ts source.
#
# Why — and why the obvious reason for it is wrong — is in AGENTS.md, under
# Checks. The `--config` flag needs the `=`; the measured behaviour of the
# space form is documented there too.
coverage = false
